# M3 synthetic fixture boundary

Everything in this directory is synthetic and contains no client, project, user, document,
credential, signed URL, provider prompt, or provider response. The JSON files define promotion suite
metadata only. They intentionally cannot promote an OCR or AI configuration without the separately
authorized exact configuration digest and controlled evaluation environment.
